#include <stdio.h>
#include <stdbool.h>
int main(){
    int marks = 98;
    float per = 85;
    printf("my marks is;%d and my percentage is %f",marks, per);
    bool iswalking = false;
    printf("\n iswalking:%d",iswalking);
}